import React from 'react';

const Background = ({ color }) =>
    <div style={{ backgroundColor: color, height: 100, width: '100%', transition: 'background-color 0.3s ease' }}/>

export default Background;
