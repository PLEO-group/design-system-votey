# Angular

- Entry point: `<package/angular>`
- Token/style outputs: `<public paths>`
- SVG registry/provider: `<API and bootstrap path>`
- Theme runtime: `<API and bootstrap path>`
- Responsive runtime/mixins: `<API or not-applicable>`
- Component source/public API: `<paths or not-applicable>`
- Consumer asset copying: `<configuration>`

## Isolation and validation

Kod Angular, DI, komponenty, typy i zależności frameworka nie mogą przeciekać do
root ani React entry pointu. Podaj dokładne komendy build/test/pack i smoke test.
